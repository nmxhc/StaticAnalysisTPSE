package AST.Expressions;

/**
 * Enum representing the boolean operations, i.e. ==, !=, <, >, <=, >=
 */
public enum ComparisonOperator {
    EQ, // ==
    NEQ, // !=
    LT, // <
    GT, // >
    LEQ, // <=
    GEQ // >=
}

